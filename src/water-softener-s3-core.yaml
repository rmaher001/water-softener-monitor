# ESPHome Water Softener Salt Monitor - v2.0.3
# GitHub: https://github.com/rmaher001/water-softener-monitor
#
# Distance-based monitoring with step-based regeneration detection
# Detects permanent distance increases after regeneration cycles (salt consumption)
# All advanced analytics handled in Home Assistant

esp32:
  board: m5stack-atoms3
  variant: esp32s3
  framework:
    type: esp-idf

# Preferences with debounced flash writes
preferences:
  flash_write_interval: 5s

# WiFi configuration moved to individual config files
captive_portal:

# Enable Improv via Serial for web installer WiFi configuration
improv_serial:

# Enable BLE Improv for wireless WiFi configuration
esp32_improv:
  authorizer: none

# Enable logging
logger:
  baud_rate: 115200
  deassert_rts_dtr: true  # Required for ESP32-S3 USB CDC compatibility

# Web server for standalone configuration
web_server:
  port: 80
  version: 3

# I2C for the ToF sensor (ATOM S3 Lite Grove port)
i2c:
  sda: GPIO2
  scl: GPIO1
  scan: true

# Status LED (SK6812 RGB on GPIO35)
light:
  - platform: esp32_rmt_led_strip
    id: status_led
    name: "Status LED"
    rgb_order: GRB
    pin: GPIO35
    num_leds: 1
    chipset: SK6812
    restore_mode: ALWAYS_OFF
    effects:
      - pulse:
          name: "Slow Pulse"
          transition_length: 1.5s
          update_interval: 2s
          min_brightness: 10%
          max_brightness: 100%

# Global variables
globals:
  - id: last_valid_status
    type: std::string
    restore_value: yes
    initial_value: '"Checking..."'

  # Step-based regeneration detection
  - id: step_baseline
    type: float
    restore_value: yes
    initial_value: '0.0'

  - id: step_state
    type: int
    restore_value: yes
    initial_value: '0'

  # Hours accumulated in CONFIRMING state — driven by the 1-hour interval,
  # so reboots pause (not reset) the 6-hour confirmation window.
  - id: step_confirm_hours
    type: int
    restore_value: yes
    initial_value: '0'

  # Hours since last regeneration (incremented hourly, survives reboots)
  - id: hours_since_regen
    type: int
    restore_value: yes
    initial_value: '9999'

# Configurable settings
number:
  - platform: template
    name: "Tank Height"
    id: tank_height_cm
    entity_category: "config"
    optimistic: true
    restore_value: true
    initial_value: 100
    min_value: 30
    max_value: 150
    step: 1
    unit_of_measurement: "cm"
    icon: "mdi:arrow-expand-vertical"
    mode: box

  - platform: template
    name: "Refill Threshold Distance"
    id: refill_threshold_cm
    entity_category: "config"
    optimistic: true
    restore_value: true
    initial_value: 43
    min_value: 10
    max_value: 120
    step: 1
    unit_of_measurement: "cm"
    icon: "mdi:arrow-expand-down"
    mode: box

  - platform: template
    name: "Update Interval"
    id: update_interval_seconds
    entity_category: "config"
    optimistic: true
    restore_value: true
    initial_value: 60
    min_value: 30
    max_value: 300
    step: 30
    unit_of_measurement: "s"
    icon: "mdi:timer-outline"
    mode: box

  - platform: template
    name: "Regen Step Threshold"
    id: regen_step_threshold
    entity_category: "config"
    optimistic: true
    restore_value: true
    initial_value: 2.0
    min_value: 1.0
    max_value: 10.0
    step: 0.5
    unit_of_measurement: "cm"
    icon: "mdi:stairs"
    mode: box

  - platform: template
    name: "Regen Confirmation Hours"
    id: regen_confirmation_hours
    entity_category: "config"
    optimistic: true
    restore_value: true
    initial_value: 6
    min_value: 1
    max_value: 24
    step: 1
    unit_of_measurement: "h"
    icon: "mdi:timer-check-outline"
    mode: box

  - platform: template
    name: "Regen Overdue Days"
    id: regen_overdue_days
    entity_category: "config"
    optimistic: true
    restore_value: true
    initial_value: 20
    min_value: 5
    max_value: 30
    step: 1
    unit_of_measurement: "days"
    icon: "mdi:calendar-alert"
    mode: box

# Switches
switch:
  - platform: template
    name: "Fast Polling Mode"
    id: fast_polling_mode
    icon: "mdi:speedometer"
    optimistic: true
    on_turn_on:
      - delay: 300s  # Auto-disable after 5 minutes
      - switch.turn_off: fast_polling_mode
    on_turn_off:
      - lambda: |-
          ESP_LOGI("config", "Fast polling disabled");

  # Regen Test Mode: makes CONFIRMING progress tick every 60s instead of
  # every 3600s. Lets a bench unit validate the 6-hour confirmation + reboot
  # persistence in ~6 minutes. Auto-disables after 30 minutes so it can't
  # be left on by accident.
  - platform: template
    name: "Regen Test Mode"
    id: regen_test_mode
    icon: "mdi:speedometer-medium"
    optimistic: true
    on_turn_on:
      - lambda: |-
          ESP_LOGW("step", "Regen test mode ENABLED — confirmation ticks every 60s");
      - delay: 1800s  # Auto-disable after 30 minutes
      - switch.turn_off: regen_test_mode
    on_turn_off:
      - lambda: |-
          ESP_LOGI("step", "Regen test mode disabled");

# Scripts
script:
  # Shared CONFIRMING-state progression logic, called by both the 3600s
  # production interval and the 60s test-mode interval. Keeps a single code
  # path for confirmation, regardless of tick rate.
  - id: advance_regen_confirmation
    then:
      - lambda: |-
          if (id(step_state) != 1) return;

          float distance = id(salt_distance_raw).state;
          if (isnan(distance)) return;

          float baseline = id(step_baseline);
          float step_thresh = id(regen_step_threshold).state;

          // Guard: if distance slipped back near baseline, abandon.
          if (distance < baseline + step_thresh * 0.5) {
            ESP_LOGI("step", "Tick check: step abandoned, distance %.1f < baseline %.1f + thresh/2", distance, baseline);
            id(step_state) = 0;
            id(step_confirm_hours) = 0;
            return;
          }

          id(step_confirm_hours) = id(step_confirm_hours) + 1;
          int target_hours = (int)id(regen_confirmation_hours).state;
          ESP_LOGI("step", "Confirming regen: %d / %d %s at %.1f cm (baseline %.1f)",
                   id(step_confirm_hours), target_hours,
                   id(regen_test_mode).state ? "ticks [TEST]" : "hours",
                   distance, baseline);

          if (id(step_confirm_hours) >= target_hours) {
            ESP_LOGI("step", "Regeneration confirmed! Baseline: %.1f -> %.1f cm (step %.1f, after %d ticks)",
                     baseline, distance, distance - baseline, id(step_confirm_hours));
            id(step_baseline) = distance;
            id(hours_since_regen) = 0;
            id(step_state) = 0;
            id(step_confirm_hours) = 0;

            id(regen_overdue).publish_state(false);
            id(last_regen_sensor).publish_state(0.0);
          }

# Buttons
button:
  - platform: template
    name: "Set Default Thresholds"
    id: set_default_thresholds
    icon: "mdi:auto-fix"
    on_press:
      - lambda: |-
          float tank_height = id(tank_height_cm).state;
          float new_threshold = tank_height * 0.43;
          id(refill_threshold_cm).publish_state(new_threshold);
          ESP_LOGI("config", "Default thresholds set: threshold=%.1fcm (43%% of %.0fcm tank height)", new_threshold, tank_height);

  - platform: template
    name: "Mark Regeneration Now"
    id: mark_regen_now
    icon: "mdi:water-sync"
    on_press:
      - lambda: |-
          id(hours_since_regen) = 0;
          id(step_baseline) = id(salt_distance_raw).state;
          id(step_state) = 0;
          id(step_confirm_hours) = 0;
          id(regen_overdue).publish_state(false);
          id(last_regen_sensor).publish_state(0.0);
          ESP_LOGI("step", "Manual regen mark: baseline=%.1f cm", id(step_baseline));

  - platform: restart
    name: "Restart"
    icon: "mdi:restart"

# VL53L0X Distance Sensor
sensor:
  - platform: vl53l0x
    name: "Distance to Salt"
    id: salt_distance_raw
    address: 0x29
    update_interval: never  # Controlled by interval component
    long_range: true
    unit_of_measurement: "cm"
    accuracy_decimals: 1
    filters:
      - multiply: 100  # Convert meters to cm
      - filter_out: nan
      - clamp:
          min_value: 5.0
          max_value: 120.0
          ignore_out_of_range: true
      - median:
          window_size: 3
          send_every: 1
    on_value:
      then:
        - component.update: salt_status_text
        - lambda: |-
            // Step-based regeneration detection (persisted-state variant, v2.0.1)
            // Detects permanent distance increases after regeneration (salt consumption).
            // CONFIRMING progress is counted in whole hours by the 1-hour interval,
            // so reboots pause the confirmation window instead of resetting it.
            float distance = id(salt_distance_raw).state;
            if (isnan(distance)) return;

            float baseline = id(step_baseline);
            float step_thresh = id(regen_step_threshold).state;

            const float REFILL_DROP_CM = 10.0;
            const float EMA_ALPHA = 0.05;
            const float NOISE_BAND_CM = 1.5;

            // === INITIALIZE BASELINE ON FIRST VALID READING ===
            if (baseline <= 0.0) {
              id(step_baseline) = distance;
              ESP_LOGI("step", "Baseline initialized to %.1f cm", distance);
              return;
            }

            // === SALT REFILL DETECTION (large drop) ===
            if (distance < baseline - REFILL_DROP_CM) {
              ESP_LOGI("step", "Salt refill detected: %.1f -> %.1f cm (drop %.1f)",
                       baseline, distance, baseline - distance);
              id(step_baseline) = distance;
              id(step_state) = 0;
              id(step_confirm_hours) = 0;
              return;
            }

            // === STATE MACHINE ===
            if (id(step_state) == 0) {
              // MONITORING state: update EMA baseline for readings within noise band
              if (fabs(distance - baseline) < NOISE_BAND_CM) {
                id(step_baseline) = EMA_ALPHA * distance + (1.0 - EMA_ALPHA) * baseline;
              }

              // Check for step increase -> enter CONFIRMING
              if (distance > baseline + step_thresh) {
                id(step_state) = 1;
                id(step_confirm_hours) = 0;
                ESP_LOGI("step", "Potential step: %.1f cm (baseline %.1f + thresh %.1f = %.1f)",
                         distance, baseline, step_thresh, baseline + step_thresh);
              }

            } else if (id(step_state) == 1) {
              // CONFIRMING state: if distance drops back near baseline, abandon
              if (distance < baseline + step_thresh * 0.5) {
                ESP_LOGI("step", "Step not confirmed, returned near baseline: %.1f cm", distance);
                id(step_state) = 0;
                id(step_confirm_hours) = 0;
              }
              // Hour-counter progression and final confirmation happen in the
              // 1-hour interval below; nothing else to do per-reading.
            }

  # WiFi signal strength
  - platform: wifi_signal
    name: "WiFi Signal"
    update_interval: 60s
    icon: "mdi:wifi"

  # Days since last detected regeneration
  - platform: template
    name: "Last Regeneration"
    id: last_regen_sensor
    icon: "mdi:water-sync"
    unit_of_measurement: "days"
    accuracy_decimals: 1
    update_interval: 3600s
    lambda: |-
      if (id(hours_since_regen) >= 9999) {
        return NAN;  // Never detected
      }
      return id(hours_since_regen) / 24.0;

# Binary sensors
binary_sensor:
  - platform: template
    name: "Sensor Out of Range"
    id: sensor_out_of_range
    icon: "mdi:alert-circle"
    lambda: |-
      const float SENSOR_MIN_RANGE = 5.0;   // cm
      const float SENSOR_MAX_RANGE = 120.0; // cm
      float current_distance = id(salt_distance_raw).state;

      if (isnan(current_distance)) {
        return true;
      } else if (current_distance < SENSOR_MIN_RANGE || current_distance > SENSOR_MAX_RANGE) {
        return true;
      }
      return false;

  - platform: template
    name: "Regeneration Overdue"
    id: regen_overdue
    icon: "mdi:calendar-alert"
    device_class: problem
    lambda: |-
      if (id(hours_since_regen) >= 9999) {
        return false;  // Never detected yet, not overdue
      }
      int overdue_hours = (int)(id(regen_overdue_days).state * 24);
      return id(hours_since_regen) >= overdue_hours;

# Status text
text_sensor:
  - platform: template
    name: "Salt Status"
    id: salt_status_text
    icon: "mdi:information-variant"
    update_interval: never
    lambda: |-
      // Status hysteresis configuration
      const float STATUS_HYSTERESIS_CM = 2.0;  // Dead zone to prevent status bouncing

      float distance = id(salt_distance_raw).state;
      float threshold = id(refill_threshold_cm).state;

      std::string new_status;
      std::string current_status = id(last_valid_status);

      if (isnan(distance)) {
        new_status = "Checking...";
      } else {
        // First boot: Initialize status directly without hysteresis
        if (current_status == "Checking...") {
          new_status = (distance >= threshold) ? "Refill" : "Good";
        }
        // Apply hysteresis based on current status
        else if (current_status == "Good") {
          // Need to exceed threshold + hysteresis to change to Refill
          if (distance >= (threshold + STATUS_HYSTERESIS_CM)) {
            new_status = "Refill";
          } else {
            new_status = "Good";
          }
        } else {
          // Currently "Refill" - need to drop below threshold - hysteresis for Good
          if (distance < (threshold - STATUS_HYSTERESIS_CM)) {
            new_status = "Good";
          } else {
            new_status = "Refill";
          }
        }
      }

      // Log status changes and update LED
      if (new_status != current_status) {
        ESP_LOGI("status", "Status change: %s -> %s", current_status.c_str(), new_status.c_str());

        // Update status LED based on new status
        if (new_status == "Refill") {
          // Slow pulsing red for refill needed
          auto call = id(status_led).turn_on();
          call.set_rgb(1.0, 0.0, 0.0);  // Red
          call.set_brightness(0.5);      // 50% max brightness
          call.set_effect("Slow Pulse");
          call.perform();
        } else if (new_status == "Good") {
          // Solid green for good status
          auto call = id(status_led).turn_on();
          call.set_rgb(0.0, 1.0, 0.0);  // Green
          call.set_brightness(0.35);     // 35% brightness
          call.set_effect("None");
          call.perform();
        } else {
          // Turn off for checking/unknown status
          auto call = id(status_led).turn_off();
          call.perform();
        }
      }

      id(last_valid_status) = new_status;
      return {new_status};

  - platform: template
    name: "Firmware Version"
    id: firmware_version
    icon: "mdi:chip"
    update_interval: never
    lambda: |-
      return {"${version}"};

# Update intervals
interval:
  # Fast polling for setup/calibration
  - interval: 100ms
    then:
      - if:
          condition:
            switch.is_on: fast_polling_mode
          then:
            - component.update: salt_distance_raw

  # Production hourly tick: always increments regen tracking counter.
  # CONFIRMING progress is delegated to the advance_regen_confirmation script
  # so the same code path is used in test mode (60s tick) and prod (3600s tick).
  - interval: 3600s
    then:
      - lambda: |-
          // --- Regen overdue counter (always runs) ---
          if (id(hours_since_regen) < 9999) {
            id(hours_since_regen) = id(hours_since_regen) + 1;
            int overdue_hours = (int)(id(regen_overdue_days).state * 24);

            if (id(hours_since_regen) >= overdue_hours) {
              id(regen_overdue).publish_state(true);
              ESP_LOGW("step", "Regeneration overdue! %d hours since last detected step", id(hours_since_regen));
            }
          }
      # Advance confirmation ONLY when not in test mode (test mode uses the
      # 60s interval below for the same purpose).
      - if:
          condition:
            lambda: 'return !id(regen_test_mode).state;'
          then:
            - script.execute: advance_regen_confirmation

  # Test-mode fast tick: when Regen Test Mode is ON, advance CONFIRMING
  # progress every 60s instead of every 3600s — lets a bench unit validate
  # the 6-hour confirmation + reboot persistence in 6 minutes.
  - interval: 60s
    then:
      - if:
          condition:
            lambda: 'return id(regen_test_mode).state;'
          then:
            - script.execute: advance_regen_confirmation

  # Normal update interval
  - interval: 1s
    then:
      - lambda: |-
          static unsigned long last_update = 0;
          unsigned long current_time = millis() / 1000;
          unsigned long interval_sec = (unsigned long)id(update_interval_seconds).state;

          if (!id(fast_polling_mode).state && (current_time - last_update >= interval_sec)) {
            last_update = current_time;
            id(salt_distance_raw).update();
          }

# On boot - take immediate reading, set firmware version, and initialize LED
esphome:
  on_boot:
    priority: -100  # After other components initialized
    then:
      - delay: 2s  # Wait for sensor to stabilize
      - component.update: salt_distance_raw
      - component.update: firmware_version
      - delay: 500ms  # Wait for status to be computed
      - lambda: |-
          ESP_LOGI("step", "Boot: baseline=%.1f cm, hours_since_regen=%d, state=%d, confirm_hours=%d",
                   id(step_baseline), id(hours_since_regen), id(step_state), id(step_confirm_hours));
          // Initialize LED based on current status
          std::string status = id(last_valid_status);
          if (status == "Refill") {
            auto call = id(status_led).turn_on();
            call.set_rgb(1.0, 0.0, 0.0);
            call.set_brightness(0.5);
            call.set_effect("Slow Pulse");
            call.perform();
          } else if (status == "Good") {
            auto call = id(status_led).turn_on();
            call.set_rgb(0.0, 1.0, 0.0);
            call.set_brightness(0.35);
            call.set_effect("None");
            call.perform();
          }
